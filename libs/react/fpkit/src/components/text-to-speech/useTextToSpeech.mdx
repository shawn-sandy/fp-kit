# useTextToSpeech

`useTextToSpeech` is a custom React hook that provides text-to-speech functionality. It allows you to easily integrate speech synthesis into your React components.

## Import

```tsx
import { useTextToSpeech } from '@fpkit/react';
```

## Usage

## Basic Usage
